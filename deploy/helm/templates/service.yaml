apiVersion: v1
kind: Service
metadata:
  name: {{ include "rigorousrag.fullname" . }}
  labels:
    {{- include "rigorousrag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rigorousrag.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
