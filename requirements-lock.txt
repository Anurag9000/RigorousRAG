# Isolated release-lock tooling only; install with pip>=25,<26 as documented.
# The application runtime and development environment do not depend on these packages.
pip-tools==7.6.0
typing-extensions>=4.12,<5
